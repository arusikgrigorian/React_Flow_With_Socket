export enum SOCKET_ENDPOINTS {
  socket = "socket",
}

export enum HTTP_ENDPOINTS {
  gembaNote = "five-w-two-h-note",
}

export enum EventSource {
  gemba = "gemba",
  "remove-gemba" = "remove-gemba",
}

export enum Event {
  input = "input",
  resize = "resize",
  position = "position",
  addition = "addition",
  deletion = "deletion",
}

export enum ROOM {
  note = "note",
}
